# Dockerfile stub — populated when this service gets its executable in a later story.
# Same multi-stage shape as services/gateway/Dockerfile; enable when the service has a main class.

FROM eclipse-temurin:25-jre-alpine
RUN echo "auth-service Dockerfile is a stub. Populated in Epic 1 Stories 1.2/1.3." && exit 1
